// Canonical production URL for the app — the Vercel "cash-flow" project.
// Kept in one place so invite links and help text never drift to an old deployment URL.
export const APP_URL = "https://cash-flow-six-beta.vercel.app";
export const APP_HOST = "cash-flow-six-beta.vercel.app";

// Where an invited friend lands (the auth / sign-up screen).
export const INVITE_URL = `${APP_URL}/auth`;
